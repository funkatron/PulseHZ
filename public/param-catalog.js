/**
 * Modulatable parameters (browser + future export).
 * Ids must match `target.paramId` in controls and server payloads.
 */

export const LAYER_PARAMS = {
  opacity: { paramId: "opacity", min: 0, max: 1, default: 1 },
};
